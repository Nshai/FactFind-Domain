SET QUOTED_IDENTIFIER OFF
GO
SET ANSI_NULLS OFF
GO
CREATE VIEW [dbo].[VwDeletedRefVAT]
AS
  SELECT 
    RefVATId, 
    'AuditId' = MAX(AuditId)
  FROM TRefVATAudit
  WHERE StampAction = 'D'
  GROUP BY RefVATId


GO
